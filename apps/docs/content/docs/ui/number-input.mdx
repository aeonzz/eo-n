---
title: Number Input
description: A UI component for adjusting numerical values with buttons, a text field, and an interactive scrub area for fine-tuned control.
preview: true
links:
  doc: https://base-ui.com/react/components/number-field
  api: /docs/ui/number-input#api-reference
---

<ComponentTabs name="number-input-demo" />

## Installation

### CLI

```package-install
npx shadcn@latest add "https://eo-n.vercel.app/r/number-input"
```

### Manual

<Steps>
  <Step>
    Install the following dependencies:

    ```package-install
    @base-ui-components/react
    ```

  </Step>
  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="number-input" />

  </Step>
  
  <Step>Update the import paths to match your project setup.</Step>

</Steps>

## Usage

Import all parts and piece them together.

```tsx
import {
  NumberInput,
  NumberInputDecrement,
  NumberInputField,
  NumberInputGroup,
  NumberInputIncrement,
  NumberInputScrubArea,
  NumberInputScrubAreaCursor,
} from "@/components/ui/number-input";
```

```tsx
<NumberInput>
  <NumberInputScrubArea>
    <Label>Age</Label>
    <NumberInputScrubAreaCursor />
  </NumberInputScrubArea>
  <NumberInputGroup>
    <NumberInputDecrement />
    <NumberInputField />
    <NumberInputIncrement />
  </NumberInputGroup>
</NumberInput>
```

## Examples

### Bounded Number Input

<ComponentTabs name="number-input-min-max" />

### Disabled

<ComponentTabs name="number-input-disabled" />

### Scrub Direction

<ComponentTabs name="number-input-scrub-direction" />

## API Reference

### Root

The main wrapper for the number input component.

<DataAttributesTable
  attributes={[
    {
      title: "[data-slot]",
      value: ["number-input"],
    },
  ]}
/>

### Scrub Area

An interactive area where users can click and drag to change the value.

<DataAttributesTable
  attributes={[
    {
      title: "[data-slot]",
      value: ["dialog-trigger"],
    },
  ]}
/>

### Group

A wrapper that contains the number input field and controls.

<DataAttributesTable
  attributes={[
    {
      title: "[data-slot]",
      value: ["number-input-group"],
    },
  ]}
/>

### Field

The input field where users can manually enter a number.

<DataAttributesTable
  attributes={[
    {
      title: "[data-slot]",
      value: ["number-input-field"],
    },
  ]}
/>

### Decrement

A button to decrease the value.

<DataAttributesTable
  attributes={[
    {
      title: "[data-slot]",
      value: ["number-input-decrement"],
    },
  ]}
/>

### Increment

A button to increase the value.

<DataAttributesTable
  attributes={[
    {
      title: "[data-slot]",
      value: ["number-input-increment"],
    },
  ]}
/>

### Additional Resources

#### API

For complete details on all available props and configuration options, see the [Base UI Number Field API Reference](https://base-ui.com/react/components/number-field).
